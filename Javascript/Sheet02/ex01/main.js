function formatearA(nombre, apellido){
    return `${apellido}, ${nombre}`
}

const formatearB = function(nombre, apellido){
    return `${apellido}, ${nombre}`
}

const formatearC = (nombre, apellido) => `${apellido}, ${nombre}`
